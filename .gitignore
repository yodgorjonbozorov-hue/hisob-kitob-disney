node_modules
.next
out
build
dist
*.db
*.db.backup
*.db-journal
.env
.env*.local
npm-debug.log*
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Vaqtinchalik (scratch) skriptlar — commit qilinmaydi
scratch-*

# Baza zaxiralari — MIJOZ MA'LUMOTI, hech qachon repoga tushmaydi
prisma/backups/

# Faqat mahalliy Windows muhitida npm o'rniga pnpm ishlatilgani uchun (README'ga qarang) —
# Vercel'ning o'z (Linux) muhitida standart npm bilan o'rnatilishi uchun bularni commit qilmaymiz.
pnpm-lock.yaml
pnpm-workspace.yaml

# Postgres sinovi/generatsiyasi uchun vaqtinchalik sxema nusxalari
prisma/.pg-*.tmp.prisma

# Playwright (brauzer testlari)
/test-results/
/playwright-report/
/blob-report/
